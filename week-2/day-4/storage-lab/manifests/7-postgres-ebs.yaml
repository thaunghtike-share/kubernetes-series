apiVersion: v1
kind: Service
metadata:
  name: postgres-ebs
  namespace: storage-lab
spec:
  clusterIP: None
  selector:
    app: postgres-ebs
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-ebs
  namespace: storage-lab
spec:
  serviceName: postgres-ebs
  replicas: 1
  selector:
    matchLabels:
      app: postgres-ebs
  template:
    metadata:
      labels:
        app: postgres-ebs
    spec:
      containers:
        - name: postgres
          image: postgres:17
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              value: password123
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: ebs-gp3
        resources:
          requests:
            storage: 8Gi
