apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops-demo-app.fullname" . }}
  labels:
    {{- include "gitops-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitops-demo-app.selectorLabels" . | nindent 4 }}
