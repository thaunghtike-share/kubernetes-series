apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops-demo-frontend.fullname" . }}
  labels:
    {{- include "gitops-demo-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitops-demo-frontend.selectorLabels" . | nindent 4 }}
